// Tipos de dados primitivos: String, number, undefined, null, boolean

const nome = 'Luiz'; // Strings com aspas simples
const nome1 = "Luiz"; // com aspas duplas
const nome2 = `Luiz`; // com crases, nesta podemos usar o Template Strings
const num1 = 10; // Number
const num2 = 10.52; //Number
let nomeAluno; // A variável declarada sem o valor ( = 'Mario';) é lida pelo sistema como 'undefined' ou seja, não aponta pra nenhum local na memória...
const sobrenomeAluno = null; // O 'null' ou nulo, serve para dizer que não quero que a variável aponte pra um local na memória, ou pra dizer que ela não aponta pra nenhum local na memória ...
const aprovado = true; // ou
//const boolean = false; // Boolean = true, false (lógico) Ex.:
//const aprovado = true; // Se for verdade, o sistema imprime o Diploma, se for 'false', imprime a matrícula de recuperação, por exemplo.

console.log(typeof aprovado, aprovado);



